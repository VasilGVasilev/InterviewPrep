[cd android && ./gradlew clean && ./gradlew :app:bundleRelease](https://stackoverflow.com/questions/51610420/deprecated-gradle-features-were-used-in-this-build-making-it-incompatible-with)

run 
```zsh
npx react-native run-android -- --warning-mode=all
```