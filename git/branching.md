Branching in git is actually conceptually making a sticker on the current branch. Thus, even the command for new branch is merely noting the name, it does not have any note on basing the new branch on the current explicitly with name:

git checkout -b NEW_BRANCH_NAME